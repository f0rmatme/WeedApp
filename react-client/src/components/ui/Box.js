import styled from "@emotion/styled";
import {
  typography,
  space,
  color,
  layout,
  border,
  background,
  position,
  shadow,
  flexbox
} from "styled-system";

const Box = styled("div")(
  typography,
  space,
  color,
  layout,
  border,
  background,
  position,
  shadow,
  flexbox
);

export default Box;
