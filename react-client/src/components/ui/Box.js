import styled from "@emotion/styled";
import {
  typography,
  space,
  color,
  layout,
  border,
  background,
  position,
  shadow
} from "styled-system";

const Box = styled("div")(
  typography,
  space,
  color,
  layout,
  border,
  background,
  position,
  shadow
);

export default Box;
