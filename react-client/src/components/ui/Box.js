import styled from "@emotion/styled";
import {
  typography,
  space,
  color,
  layout,
  border,
  background,
  position
} from "styled-system";

const Box = styled("div")(
  typography,
  space,
  color,
  layout,
  border,
  background,
  position
);

export default Box;
